var component = component || {};
component.channel = component.channel || {};
component.channel.FormToDataTable = function (sourceForm, targetDataTable) {
    let _this = this;

    sourceForm.on("form.add.success", function (successData) {

    });

    sourceForm.on("form.add.error", function (ex) {

    });

    sourceForm.on("form.edit.success", function (successData) {

    });

    sourceForm.on("form.edit.error", function (ex) {

    });

    sourceForm.on("form.save.success", function (successData) {

    });

    sourceForm.on("form.save.error", function (ex) {

    });
}